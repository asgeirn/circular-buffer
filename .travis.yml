language: java

deploy:
  provider: script
  script: "mvn versions:set -Dversion=$TRAVIS_TAG && mvn deploy -Dregistry=https://maven.pkg.github.com/asgeirn -Dtoken=$GH_TOKEN"
  skip_cleanup: true
  on:
    tags: true
